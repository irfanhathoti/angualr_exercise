import { RegisterDetailDirective } from './register-detail.directive';

describe('RegisterDetailDirective', () => {
  it('should create an instance', () => {
    const directive = new RegisterDetailDirective();
    expect(directive).toBeTruthy();
  });
});
